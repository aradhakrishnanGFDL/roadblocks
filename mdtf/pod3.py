#read csv (so framework writes json into a csv, why? perhaps more language independent, for large ensemble runs POD developer could write a helper utility to generate this csv, why separate file -> can help with preprocessor disable or run on demand function, $i
# and a good flow to use of intake catalogs if we get to it later. 
#check how the catalog looks like today 
